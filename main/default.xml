<Layer
    Type = "ActorFrame"
    Condition = "(function(self)
        _G.sudo = {}
        assert(loadfile('lib/actorgen.lua'))()
        return true
    end)()"
    InitCommand = "%function(self)
        sudo.fg = self
		sudo.ActorList = Def.ActorFrame {
            assert(loadfile('lib/stdlib.lua'))(),
            sudo(assert(loadfile('lua/nodes.lua')))(),
			sudo(assert(loadfile('lua/mods.lua')))(),
		}
        self:sleep(9e9)
    end"
	OnCommand = "%function(self)
		self:playcommand('Prep')
	end"
><children>
	<Layer 
		Name="Root"
		File="../main/actors.xml"
		Condition="actorgen.Template(sudo.ActorList)"
		InitCommand="%actorgen.InitCmd"
	/>
</children></Layer>